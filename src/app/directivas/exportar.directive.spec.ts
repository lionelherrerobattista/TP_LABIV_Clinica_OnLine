import { ExportarDirective } from './exportar.directive';

describe('ExportarDirective', () => {
  it('should create an instance', () => {
    const directive = new ExportarDirective();
    expect(directive).toBeTruthy();
  });
});
