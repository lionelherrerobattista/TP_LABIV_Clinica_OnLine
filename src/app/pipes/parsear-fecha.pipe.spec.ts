import { ParsearFechaPipe } from './parsear-fecha.pipe';

describe('ParsearFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new ParsearFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
